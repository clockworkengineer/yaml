[package]
name = "yaml_embedded_systems"
version = "0.2.1"
edition = "2021"

[dependencies]
yaml_lib = { path = "../../library", default-features = false, features = [
    "embedded",
    "alloc",
] }

[features]
default = ["std"]
std = ["yaml_lib/std"]
