# DRY Refactor Plan for anchors.rs

## Overview
This plan identifies and addresses code duplication and opportunities for DRY (Don't Repeat Yourself) improvements in `parser/document/anchors.rs`. The goal is to centralize common logic, improve maintainability, and ensure consistent error handling and traversal patterns.

## Key DRY Refactor Opportunities

1. **Centralize Node Traversal Patterns**
   - The file uses similar traversal logic (closures + `visit`/`visit_mut`) in multiple functions.
   - Plan: Abstract common traversal patterns into reusable helpers in a `utils/anchors_helpers.rs` or similar module.

2. **Unify Error Handling**
   - Error construction and propagation is repeated (manual `Option<String>`, then `if let Some(e)` pattern).
   - Plan: Create a helper to wrap closure-based traversals and propagate errors in a DRY way.
   - Use centralized error messages and types from `error_helpers`.

3. **Deduplicate Anchor/Alias/Merge Logic**
   - Similar logic for anchor lookup, alias replacement, and merge expansion is repeated (e.g., anchor lookup, error for missing anchor, type checks).
   - Plan: Extract anchor lookup and merge expansion into small, reusable helpers.

4. **Consistent Logging and Debug Output**
   - If debug output is needed, use a macro (e.g., `anchors_debug!`) for all logging, similar to `lexer_debug!`.

5. **Test Utilities**
   - If test code repeats anchor/alias/merge setup, move to a test helper module.

## Concrete Refactor Steps

1. **Abstract Node Traversal**
    - Create a helper for error-propagating node traversal (e.g., `traverse_with_error`).
    - Refactor `collect_anchors`, `replace_aliases`, and `expand_merge_keys` to use this helper.

2. **Centralize Error Construction**
    - Move all error construction to `utils/error_helpers.rs`.
    - Replace manual error string creation with calls to these helpers.

3. **Extract Anchor/Alias/Merge Helpers**
    - Create small helpers for anchor lookup, alias replacement, and merge expansion.
    - Use these helpers in the main functions to reduce duplication.

4. **Add Logging Macro (Optional)**
    - If logging is needed, add an `anchors_debug!` macro and use it for all debug output.

5. **Refactor and Test**
    - Refactor each function to use the new helpers.
    - Ensure all tests pass and add new tests for edge cases if needed.

## Expected Outcomes
- Reduced code duplication and improved maintainability in `anchors.rs`.
- Centralized, reusable helpers for node traversal, error handling, and anchor/alias/merge logic.
- Consistent and DRY error reporting and (optional) logging.
- Cleaner, more maintainable test code.

---
*Generated by GitHub Copilot on 2026-01-26*
